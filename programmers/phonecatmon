# https://programmers.co.kr/learn/courses/30/lessons/1845


def solution(nums):
    select = int(len(nums) / 2)
    kinds = len(set(nums))
    
    if kinds > select:
        return select
    else: return kinds
    
